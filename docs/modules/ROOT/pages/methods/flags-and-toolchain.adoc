= Flags and toolchain methods
:description: Toolchain lookup and mutating compile/link flags

This page will hold the full tutorial for `Toolchain`, `ReplaceFlags`, `RemoveFlags`, and the
everyday flag helpers `AppendUnique`, `MergeFlags`, and `Append` — one coherent “how do I set
flags?” story.

Until the Methods split finishes migrating prose, use
xref:methods.adoc#dialect-modules-and-flags[Methods — Dialect, modules, and flags]
and the Templates table (`Toolchain`) on the hub.

== Behaviour (draft)

[cols="1,3"]
|===
| Returns | `Toolchain` returns a toolchain object; flag helpers mutate env
| Evaluation | Immediate
| Output paths | N/A
| Progress | No
|===

Related: xref:methods/dependencies-and-profiles.adoc[Dependencies and profiles] (`BuildWith`),
xref:toolchains.adoc[Toolchains].
