= Documentation and asset methods
:description: AsciiDoc, Markdown, SCSS, and redirect capture

Builders that turn documentation and assets into variant outputs under `final/` (or related
trees). Text templates live on xref:methods/templates.adoc[Template expansion]; C++ version
sources on xref:methods/create-version.adoc[CreateVersion].

== Behaviour summary

[cols="1,3"]
|===
| Returns | Build nodes for generated artefacts
| Evaluation | Build action
| Output paths | Several emitters still use flat `\{final}/\{basename}` — nested same-basename sources can collide (https://github.com/ja11sop/cuppa/issues/233[#233])
| Progress | Yes where emitters create action nodes
|===

== Methods

[cols="2,3"]
|===
| Method | Role

| `MarkdownToHtml()` / `AsciidocToHtml()`
| Doc conversion builders

| `CompileScss()`
| SCSS → CSS

| `RunAndRedirectToFile()`
| Capture subprocess stdout to a file
|===

Until https://github.com/ja11sop/cuppa/issues/233[#233] lands, avoid feeding two
`readme.md` files from different directories into `MarkdownToHtml()` without explicit distinct
targets — they can collide on `\{final}/readme.html`.

== Related methods

* xref:methods/templates.adoc[Template expansion] — `ExpandTemplateFile()` / `RenderJinjaTemplate()`
* xref:methods/create-version.adoc[CreateVersion] — toolchain-backed version sources
* xref:methods/flags-and-toolchain.adoc[Flags and toolchain] — `Toolchain()` lookup
* xref:methods/staging-files.adoc[Staging files] — staging CSS/assets under `final/`
* xref:integration/test-docs-assets.adoc[Integration: Docs and assets] ·
  xref:integration/test-redirect.adoc[RunAndRedirectToFile]
