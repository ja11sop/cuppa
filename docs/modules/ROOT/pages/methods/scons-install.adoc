= SCons Install and InstallAs
:description: Canonical SCons install helpers in Cuppa projects

This page will hold the full tutorial for SCons `env.Install`, `env.InstallAs` (companions to Cuppa `CopyFiles` / layout under `final/`).

Until the Methods split finishes migrating prose, use xref:methods.adoc[Methods hub] — companion page (Phase 2).

== Behaviour (draft)

[cols="1,3"]
|===
| Returns | Install nodes
| Evaluation | Build action
| Output paths | Caller-chosen dest; Cuppa projects usually target variant `final/`
| Progress | When Install nodes are attached to NotifyProgress via a Cuppa method
|===

See also the hub xref:methods.adoc[Methods] and the behaviour track in the repository design plans (`method-behaviour-audit`).
