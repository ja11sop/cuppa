= Dependencies and build-profile methods
:description: BuildWith, Using, and BuildProfile

This page will hold the full tutorial for `BuildWith`, `Using`, `BuildProfile`.

Until the Methods split finishes migrating prose, use xref:methods.adoc#dependencies-and-profiles[Methods — Dependencies and profiles].

== Behaviour (draft)

[cols="1,3"]
|===
| Returns | Side effects on env / build graph
| Evaluation | Configure-time
| Output paths | N/A (dependency trees under `--dependencies-root`)
| Progress | No (env / selection only)
|===

See also the hub xref:methods.adoc[Methods] and the behaviour track in the repository design plans (`method-behaviour-audit`).
