= Methods
:description: Cuppa environment methods for building, testing, libraries, and reporting

Methods are registered on each cuppa environment and called from `sconscript` files after `Import('env')`.
They differ from raw SCons builders (`Program`, `Object`, `StaticLibrary`) by understanding variants, toolchains, progress reporting, cuppa's `_build/` layout, and (when enabled) {cpp}20 modules.

This hub is the entry point for that surface.
Child pages under xref:methods/build.adoc[methods/] hold the detailed tutorials for each topic;
the hub keeps progress overview, a topic map, and summaries that point onward.
A comprehensive method index (every name → Cuppa page or careful upstream link) lands as the
split progresses.

For the vocabulary behind methods, see xref:concepts.adoc[Concepts].
For compiler defaults applied underneath these calls, see xref:toolchains.adoc[Toolchains]
and the family pages (xref:toolchains/gcc.adoc[GCC], xref:toolchains/clang.adoc[Clang],
xref:toolchains/msvc.adoc[MSVC]).

== Topic map

[cols="1,2"]
|===
| Topic | Page

| Build (`Build`, `Compile`, `BuildLib`, …)
| xref:methods/build.adoc[Build methods]

| Test and run (`BuildTest`, `Test`, `Benchmark`, `Run`)
| xref:methods/test-run.adoc[Test and run]

| Coverage
| xref:methods/coverage.adoc[Coverage methods]

| {cpp} dialect, modules, Profiles
| xref:methods/cxx-dialect-and-modules.adoc[{cpp} dialect and modules]

| Dependencies and profiles (`BuildWith`, `Using`, …)
| xref:methods/dependencies-and-profiles.adoc[Dependencies and profiles]

| Flags and toolchain (`ReplaceFlags`, `AppendUnique`, `MergeFlags`, …)
| xref:methods/flags-and-toolchain.adoc[Flags and toolchain]

| Files, discovery, templates
| xref:methods/files-and-templates.adoc[Files and templates]

| Install (`Install`, `InstallAs`)
| xref:methods/install.adoc[Install]

| Depends (`Depends`, `Requires`, `Alias`)
| xref:methods/depends.adoc[Depends]

| Docs and assets
| xref:methods/docs-assets.adoc[Docs and assets]

| Packages
| xref:methods/packages.adoc[Packages]

| Custom commands / `cwd` / `--use-shell`
| xref:methods/custom-commands.adoc[Custom commands]
|===

== Prerequisites

* Cuppa installed and a project with `cuppa.run()` in `sconstruct` (xref:quickstart.adoc[Quickstart])
* Familiarity with `Import('env')` in a `sconscript`
* A {cpp} toolchain available to cuppa (`gcc`, `clang`, or `vc` on Windows)

== Progress tracking and variant scoping

Cuppa wires build progress through `cuppa.progress.NotifyProgress` so each build variant reports begin/start/finish/end in a predictable order.

`NotifyProgress` is attached to methods that emit build action nodes (`Build`, `Compile`, `Test`, coverage collation, doc builders, and so on).
Methods that only mutate env state or select nodes (`StdCpp`, `BuildWith`, `Filter`, `RecursiveGlob`, `ImportModules`, etc.) do **not** call it directly.

Progress nodes form a dependency chain that constrains when targets may run:

[mermaid]
....
flowchart TD
    SB[SconstructBegin] --> B[Begin sconscript]
    B --> ST[Starting variant]
    ST --> T[Method targets]
    T --> F[Finished variant]
    F --> E[End sconscript]
    E --> SE[SconstructEnd]
....

`Begin` / `End` are keyed by sconscript file.
`Starting` / `Finished` are keyed by the variant path (`parent of env['build_dir']`).
In cuppa's normal layout that path already includes the sconscript segment, so different sconscripts get distinct Starting/Finished nodes without a composite key.

Practical rules when adding or changing methods:

* If a method creates SCons action nodes, wire `NotifyProgress.add(env, targets)` (or delegate to a method that already does).
* If a method only configures env or returns filtered/existing nodes, do not add progress wiring.
* Keep progress wiring on the same variant env that creates the nodes.

== Core build methods

Detailed tutorials: xref:methods/build.adoc[Build methods].

`env.Build` → `final/`; `Compile*` mirrors objects under `working/`; `Build*Lib` installs libraries into `final/`.

== Test and run methods

Detailed tutorials: xref:methods/test-run.adoc[Test and run].

Typical spine: `BuildWith` → `BuildTest` → `CopyFiles` + `Filter` → coverage / HTML report collate.

== Custom commands and working directories

Detailed tutorials: xref:methods/custom-commands.adoc[Custom commands].

Prefer `working_dir` / `cwd=` over `cd && …`; treat `--use-shell` as an escape hatch.

== Dependencies and profiles

Detailed tutorials: xref:methods/dependencies-and-profiles.adoc[Dependencies and profiles].

`BuildWith` / `Using` / `BuildProfile` — see also xref:dependencies.adoc[Dependencies].

== Files, discovery, and templates

Detailed tutorials: xref:methods/files-and-templates.adoc[Files and templates].

`RecursiveGlob` / `GlobFiles` / `Glob` / `Filter` / `CopyFiles` / `CreateVersion` / templates.

== Flags and toolchain

Detailed tutorials: xref:methods/flags-and-toolchain.adoc[Flags and toolchain].

`AppendUnique`, `MergeFlags`, `ReplaceFlags`, `RemoveFlags`, `Toolchain`.

== Install and Depends

* xref:methods/install.adoc[Install] — `Install` / `InstallAs` vs `CopyFiles`
* xref:methods/depends.adoc[Depends] — `Depends` / `Requires` / `Alias`

== Dialect, modules, and Profiles

Detailed tutorials: xref:methods/cxx-dialect-and-modules.adoc[{cpp} dialect and modules].

See also xref:cxx-modules.adoc[{cpp} Modules] and xref:cxx-profiles.adoc[{cpp} Profiles].

== Coverage

Detailed tutorials: xref:methods/coverage.adoc[Coverage methods].

IMPORTANT: `--cov` does not imply `--test` — use `cuppa -D --cov --test …`.

== Packages

Detailed tutorials: xref:methods/packages.adoc[Packages].

See xref:packages.adoc[Publishing packages].

== Documentation and assets

Detailed tutorials: xref:methods/docs-assets.adoc[Docs and assets].

NOTE: Flat `\{final}/\{basename}` collisions for some emitters — https://github.com/ja11sop/cuppa/issues/233[#233].

[#test-reporting-plugins]
== Test reporting plugins

When the corresponding plugins are loaded (shipped with cuppa via `cuppa.method.plugins`):

* `env.GenerateHtmlTestReport(…)`
* `env.CollateTestReportIndex(…)`
* Bitten XML report generation (`GenerateBittenReport`)

These activate in concert with `--test`. Full detail remains on this hub until a dedicated child page exists; they appear in the everyday test spine on xref:methods/test-run.adoc[Test and run].

[#method-index]
== Method index

Grouped names → topic page. Prefer these links over upstream docs when both exist.

[cols="1,2"]
|===
| Group | Methods → page

| Build
| `Build`, `Compile`, `CompileStatic`, `CompileShared`, `BuildLib`, `BuildStaticLib`, `BuildSharedLib` → xref:methods/build.adoc[Build]

| Test and run
| `BuildTest`, `Test`, `BuildBenchmark`, `Benchmark`, `Run` → xref:methods/test-run.adoc[Test and run]

| Coverage
| `Coverage`, `CollateCoverageFiles`, `CollateCoverageIndex` → xref:methods/coverage.adoc[Coverage]

| Dialect / modules / Profiles
| `StdCpp`, `CxxModules`, `Module`, `HeaderUnit`, `ImportModules`, Profiles helpers → xref:methods/cxx-dialect-and-modules.adoc[Dialect and modules]

| Dependencies / profiles
| `BuildWith`, `Using`, `BuildProfile` → xref:methods/dependencies-and-profiles.adoc[Dependencies]

| Flags / toolchain
| `Toolchain`, `ReplaceFlags`, `RemoveFlags`, `AppendUnique`, `MergeFlags`, `Append` → xref:methods/flags-and-toolchain.adoc[Flags]

| Files / discovery / templates
| `RecursiveGlob`, `GlobFiles`, `Glob`, `Filter`, `CopyFiles`, `CopyFilesAs`, `TargetFrom`, `CreateVersion`, `ExpandTemplateFile`, `RenderJinjaTemplate` → xref:methods/files-and-templates.adoc[Files]

| Install
| `Install`, `InstallAs` → xref:methods/install.adoc[Install]

| Depends
| `Depends`, `Requires`, `Alias` → xref:methods/depends.adoc[Depends]

| Docs / assets
| `MarkdownToHtml`, `AsciidocToHtml`, `CompileScss`, `RunAndRedirectToFile` (+ templates / `CreateVersion`) → xref:methods/docs-assets.adoc[Docs and assets]

| Packages
| `PublishPackage`, `InstallPackage`, `Command` (publishers) → xref:methods/packages.adoc[Packages] · xref:methods/custom-commands.adoc[Custom commands]

| Custom commands
| `working_dir` / `cwd` / `--use-shell` / `Command` → xref:methods/custom-commands.adoc[Custom commands]

| Test report plugins
| `GenerateHtmlTestReport`, `CollateTestReportIndex`, `GenerateBittenReport` → <<test-reporting-plugins>>
|===

== What you have available

You can express programs, libraries, tests, and module-aware builds using cuppa methods instead of assembling builders by hand.
Next, inspect xref:toolchains.adoc[Toolchains] and the family pages so you know which flags
each compiler receives by default, and xref:cxx-modules.adoc[{cpp} Modules] if you are adopting
`import` / `export module`.
