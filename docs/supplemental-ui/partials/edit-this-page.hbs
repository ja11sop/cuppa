{{!--
  Overlay that removes the toolbar "Edit this Page" link.

  The bundle partial links page.fileUri for a worktree build and page.editUrl
  otherwise, so a playbook edit_url setting alone cannot suppress it locally.
  Rendering nothing here drops the link from every page.
--}}
