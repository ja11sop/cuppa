# Setup virtualenv if not exist
cd ..
python -m virtualenv venv
source venv/bin/activate
pip install -r requirements.txt
cd cuppa/

# Lint
flake8 cuppa
pylint -E cuppa

# Tests
pytest -m unit
pytest -m integration

# Release notes
# - Move items from [Unreleased] into a new ## [X.Y.Z] - YYYY-MM-DD section in CHANGELOG.md
# - Update compare links at the bottom of CHANGELOG.md
# - Bump cuppa/VERSION to match
# - After tagging, create the GitHub release from CHANGELOG.md for that version

# Build
rm -rf dist/ build/ && python setup.py sdist bdist_wheel

# Upload to PyPi
twine upload dist/*
