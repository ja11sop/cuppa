# Minimal cuppa example

Requires `cuppa` and `scons` on `PATH` (for example `pip install cuppa scons`), plus a C++ compiler.

```sh
cd examples/minimal
cuppa -D --dbg
cuppa -D --dbg --test
```

Build products land under `_build/`.
