[pytest]
testpaths = tests
pythonpath = .
markers =
    unit: fast tests without compiler or network
    integration: requires cuppa + SCons + C++ compiler
log_cli_level = INFO
filterwarnings =
    ignore::DeprecationWarning
